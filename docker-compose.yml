version: "3.8"
services:
  userservice:
    build:
      context: ./UserService
    environment:
      # Do NOT commit production secrets here. For local development use a local Postgres or an env file.
      - ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=minhaapp;Username=teste;Password=YourLocalPassword;SSL Mode=Require;Trust Server Certificate=true
      - FRONTEND_URL=http://localhost:3000
    ports:
      - "5000:80"
